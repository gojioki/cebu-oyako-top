# ローカル優先ワークフローへの切替メモ（2026-07-23）

## 背景

最終的な納品先はGitHub Pagesではなく、**SWELL（WordPress）の固定ページのHTMLブロック**である。そのため、これまでのgit commit・ver番号採番・タグ付け・GitHub Pages公開という運用は、日々の高速な試作・改善フェーズには重すぎる。

さらに、GitHub Pagesは独立した静的サイトであり、本番で実際に起きうる事故（SWELLのテーマCSS・ブロックエディタのサニタイズ・既存JSとの衝突）を検出する手段にはなっていない。むしろSWELLの下書き固定ページに直接試し貼りする方が、本番に近い有効な確認になる。

このメモは、上記を踏まえて「今のフェーズで何を軸に進めるか」を整理したもの。**GitHubリポジトリ本体・Pages公開設定は将来の別用途のために保持し、削除・閉鎖はしない。**

## 1. 現状で大事なこと

| 項目 | 現状の位置づけ | 理由 |
|---|---|---|
| 最終納品形態 | SWELL固定ページのHTMLブロック | 唯一の実際の着地点 |
| ローカルファイル（`modular/sections/`・`site.css`・`build.rb`） | 主軸・そのまま継続 | 実装・検証の実体はここにある |
| ローカルプレビュー確認 | 主軸・そのまま継続 | ブラウザで見た目・レスポンシブ・コンソールエラーを直接確認できる |
| WordPress(SWELL)下書き固定ページへの試し貼り | 新設・本番相当チェックとして最重要 | テーマCSS・ブロックエディタとの衝突を検出できる唯一の手段 |
| GitHub Pages公開 | 一時停止（リポジトリ・公開設定は保持） | 静的サイト単体の確認はできるが、SWELLとの衝突は検出できない |
| ver番号採番・タグ運用 | 一時停止 | 高速反復の妨げになる儀式のため |

## 2. gitが本当に効くところ

| 場面 | 効くか | 補足 |
|---|---|---|
| 壊れた時に戻す（ロールバック） | 効く | ただしリモート同期は不要。ローカルのみのgitで十分 |
| 変更履歴の記録 | 効く | 同上 |
| 2台のPC間の作業同期 | 部分的に効くが必須ではない | 単独作業者ならクラウド同期フォルダ（Dropbox/iCloud等）でも代替可。同時編集がないためマージ機能自体が不要 |
| 複数人の同時編集の調整 | 現状は該当なし | GPT側は`deliveries/`への納品のみで、同時編集はしていない |
| SWELL/WordPressとの衝突検出 | 効かない | GitHub Pagesは独立環境のため検出不可 |
| 実機・外部共有用の公開URL | 効く（用途が発生すれば） | ただし今のフェーズでは必須ではない |

## 3. 高速に進めるための理想手順

| ステップ | 内容 | 主な担当・ツール |
|---|---|---|
| 1 | 文言・構成のドラフト | GPT(work)チャット |
| 2 | 実装（sections・css編集） | **Codex**（ローカルファイル、主担当） |
| 3 | ローカルプレビューで検証（見た目・レスポンシブ・コンソールエラー） | **Codex**（ブラウザツール、主担当） |
| 3.5 | 全体版統合HTML・確認用ZIP生成 | **Codex**（Junが「全体版を作成」「ver◯として作成」「バージョンを作成」と明示した作業単位で実施） |
| 4 | 節目でWordPress(SWELL)の下書き固定ページに試し貼り | Jun（SWELL管理画面） |
| 5 | 問題があれば2〜4を繰り返す | — |
| 6 | 大きな区切りでローカルのみのgit commit（任意・スナップショット目的） | **Codex**（Junが「Gitへ保存」と明示した作業単位で実施。pull・pushはしない） |
| — | 上記全体の確認・レビュー（必要に応じて） | Claude Code |

※Claude Codeは実装の主担当ではなく、必要に応じた確認・レビュー担当（詳細はAGENTS.md「役割分担」参照）。Junが直接Codexへ承認した作業は、追加でClaude Codeの承認を必要としない。

### Codexへの指示と実行範囲

| Junの指示 | Codexが実行する範囲 |
|---|---|
| 具体的な修正依頼 | `modular/sections/`・`modular/css/`・`assets/`の編集、ローカルプレビュー・レスポンシブ検証 |
| 「全体版を作成」「ver◯として作成」「バージョンを作成」 | VERSION更新（指定時）、最終ビルド、統合HTML、`index.html`、全体確認用ZIP生成 |
| 「Gitへ保存」 | 検証済み変更のローカルcommit。pull・push・タグ付けは行わない |
| 「リモートへ保存」 | pull、検証、commit、push |
| 「公開まで進める」 | pull、VERSION更新、最終ビルド、検証、commit、同名タグ、push、GitHub Pages公開 |

## 4. 開通・準備するもの／閉じる・やめるもの

**開通・準備するもの**

| 対象 | 内容 |
|---|---|
| WordPress(SWELL)の下書き固定ページ | 非公開・確認専用として1つ用意しておく |
| ローカルのみのgit | リモートには繋がない・push/pullしない。壊れた時のロールバック用の安全網として運用 |

**やめる・一時停止するもの（削除はしない）**

| 対象 | 内容 |
|---|---|
| git push / pull による2台間同期運用 | 停止。再開したくなったら随時戻せる |
| ver番号採番・タグ付けの毎回運用 | 停止。本当に区切りをつけたい時だけ任意で実施 |
| GitHub Pagesへのpush＝公開のフロー | 停止 |

**保持するもの（削除しない）**

| 対象 | 理由 |
|---|---|
| GitHubリポジトリ本体（gojioki/cebu-oyako-top） | 将来、別の用途（複数人での同時編集・外部共有・テンプレート配布など）が発生した時にすぐ再開できるように残す |
| GitHub Pages公開設定 | 同上 |

## 今後の見直し条件

以下のいずれかが発生したら、git運用（push/pull・タグ・Pages公開）を再開する。

- 複数人が同時に同じファイルを編集する必要が出てきた
- 外部の第三者に共有できるURLが必要になった
- 法人サイト・別メディアへのテンプレート横展開を本格的に始める（AGENTS.md記載の将来計画）

リポジトリ・Pages設定は生きたまま保持しているため、いつでも元の運用に戻せる。
