# SWELLトップテンプレ作成：恒久ルール

> **本ファイルの管理者：総合スレ（Claude）のみが更新する。他スレ・他AIは読むだけで編集しない。**
> 最終更新：2026-07-20（引き継ぎ書の確定ルールを統合。マスター現在地はver19）

## 現在の優先順位

- まず「ぶっ飛びセブ島親子留学」のPC版トップページを、基準画像 `ぶっ飛びTOP2.png`・`ぶっ飛びTOP3.png` に沿って細部まで完成させる。
- ユーザーの明示指示なしに、基本デザイン、構成、カード形状を大きく変えない。指定された文言・画像・項目を既存デザインの枠内で差し替える。
- スマホ版を想定した構造にはするが、現段階の視覚調整はPC版を優先する。
- 法人サイト・別メディア向けの汎用テンプレート化は、現行トップのデザイン確定後に行う。デザイン調整中に大規模な共通化で見た目を変えない。

## 確定事項（変更しない・2026-07-19時点）

1. **運営者名＝実名「斉藤淳」**。話者ラベルは「父・斉藤」、プロフィール・運営者情報はフルネーム「斉藤 淳」。「ジュン」単体・「セブパパ」は使わない。
2. **キャラ運用の原則**：答える側＝父・斉藤本人に一本化（実写・E-E-A-T）。ドキドキママ／ホンキくんは読者の心の声の代弁専用。セブ船長・ジプニー先輩はトップページから退場済み（ver16）。
3. **禁止コピー（残存ゼロを維持）**：「無料相談する」「大成功するフィリピン〜」「英語も、親子時間も〜」「トキドキママ」「東京都Mさん」「人気コンテンツ」
4. **未公開記事への死にリンク禁止**：未公開リンクは「グレー非クリック＋『〜へ（予定）』表記」にする。記事公開のたびに`<a>`化して育てる。未公開リンクの一覧と差し替え先URL案は`タスク一覧.md`の「未公開リンク一覧」を正とする。
5. サイト内リンクは絶対URL形式（https://cebu-oyako.com/...）で統一。
6. 05実録キャプション・06すごろく・07搭乗券のコピーは実話ベースで承認済み。指示なく変更しない。

## バージョン採番（2026-07-18確定・番号衝突の再発防止）

- **VERSION番号の採番は総合スレ（Claude）のみが行う。**
- ブロックスレ（GPT work・他Claudeスレ）は番号を予約・記載しない（「ver◯適用候補」の予約も禁止）。
- 納品物は「内容＋日付」の命名（例：`cost-section-v4-20260719.zip`）とし、ver番号を含めない。
- 納品物・受領ZIPは作業フォルダ直下に散らさず、`deliveries/` 配下へ集約する。
- `modular/VERSION.txt` が出力ファイル名を決める。マスターに変更を加えるたびに総合スレが番号を上げる。

## スマホ対応の保険（2026-07-18確定）

- PC版優先・スマホ本格調整は後工程という方針は維持する。
- 各ブロックの毎回の検証幅に375pxを追加し、最低限「横スクロールなし」「画像・要素のはみ出しなし」「文字が潰れず読める」を確認する。
- 375pxではデザインの作り込みや細かな視覚調整を行わず、後工程で直せないレベルの構造破綻を早期発見する。
- HTMLは固定幅・PC専用の並びに依存させず、幅は%・max-widthを使い、グリッドは折り返し・縦積み可能な構造にする。
- スマホ用画像への差し替えが確定している箇所は、差し替え用クラスまたはpicture要素の受け皿を先に用意する。

## 共通実装ルール

このTOPページの各ブロックは、完成後に会社公式サイト・メディアサイト等へ流用する。

1. HTMLとCSSのクラス名は、すべて `btv3-` 接頭辞を付ける。他サイトやSWELLのCSSとの衝突を避ける。
2. 色、フォント、角丸、影、主要な余白などのデザイン値は、CSS冒頭の `:root` に `--btv3-*` 変数として集約する。各ブロックでは変数を参照し、色コードを直書きしない。
3. サイト固有の文言、画像、リンク先はHTML側に置き、CSSへ持ち込まない。
4. 各 `section` は単体でコピーしても崩れない自己完結構造にする。他ブロックや外側の親要素のスタイルに依存させない。
5. スマホ対応のメディアクエリは、そのブロックのCSSと同じファイル・同じ区画内で完結させる。
6. 既存コードが上記に反する場合、見た目への影響と修正案を先に示す。デザイン確定前に大規模な再構成を行わない。
7. **Webフォントは読み込まない**（システムフォント戦略。`.btv3-page`内はNoto Sans JP角ゴシック＋palt詰め）。単体プレビューにWebフォントリンクを追加しない。
8. `{{asset:...}}` のパスは `assets/` 起点で書く（接頭辞欠落はビルドがraiseで停止する）。

## ブロックスレの納品ルール（GPT work・他スレ共通）

1. **統合版HTMLを生成しない。** 納品はブロック素材（セクションHTML）＋単体プレビューのみ。
2. **CSSは追記パッチ単体ファイルで納品する。** site.css丸ごと上書き納品は他スレの追記を消すため禁止。
3. 単体プレビューは必ず `<div class="btv3-page">` で包む。プレビュー生成スクリプト等に土台CSS（:root変数）を埋め込む場合は、マスター `modular/css/site.css` の現在値と一致させる（独自の色・フォント値を書かない）。
4. 修正時は「追加・変更した箇所」と「削除した箇所」を必ず明示する。指示されていない箇所の削除・省略は禁止。
5. 既存CSS・旧資産は削除せず温存する（差し替え時は復元マーカー付きコメントアウト）。

## 検証セット（マスター適用時・総合スレが毎回実施）

- 未解決プレースホルダー0／アセット実在突合／画像全読込／コンソールエラー0
- 横スクロールなし（1600／1280／1024px＋375pxスマホ煙テスト）
- 禁止コピー残存0／既存セクションの回帰確認

## ファイルと編集手順

- 最新版のファイル名は、必ず先頭を `ver番号_` にする（※総合スレが採番したものに限る）。現在値は `modular/VERSION.txt` を正とする。
- 通常の修正は `modular/sections/` と該当ブロックのCSSだけで行う。**約17MBの統合HTMLを毎回編集しない・全文を読み込まない。**
- FVの確認は `modular/preview/ver番号_FV専用プレビュー.html` を優先する。
- 全体確認は `modular/preview/ver番号_全体軽量プレビュー.html` を使う。
- 外部へ渡すときだけ、`modular/scripts/build.rb` で画像・CSS埋め込み済みの統合HTMLを生成する（総合スレのみ）。
- SWELLへの実装を見据え、最終的には各sectionをカスタムHTMLブロックまたは再利用ブロック単位で扱えるようにする。

## コミュニケーション上の注意

- 修正前に、変える箇所と維持する箇所を短く整理する。
- ユーザーが「引き継ぎ」と明示するまでは、引き継ぎ専用ファイルを新規作成しない。
- デザイン判断では、元PNGとの一致と、端正な日本語フォント、過度に太くない見出しを優先する。
- 変更提案は先に提示し、明示的な許可を得てから実行する。

## Git運用ルール（2026-07-23更新・GPT Workの書き込み制限を一旦全解除）

> 2026-07-21確定版からの変更点：Claude Codeの利用を縮小し、日常作業をGPT Work・Claude Coworkに移す方針（2026-07-22決定）に伴い、GPT Work・他ブロックスレの書き込み範囲制限を一旦すべて解除した。下記1〜5は暫定運用。元の制限（`deliveries/`限定・Git操作Claude Code専任）に戻す場合は、その旨を明記して再確定すること。

1. **このGitリポジトリ（github.com/gojioki/cebu-oyako-top）が唯一の正。** ZIPは参照・受け渡し補助用（GPT Work等への現状共有）として使ってよいが、マスターの正本とはしない。
2. **書き込み境界：現状フリー。** GPT（ChatGPT Work）・Claude Cowork・他ブロックスレも `modular/`・`assets/`・`docs/`・VERSION採番・コミット・push を含め、リポジトリへの書き込みを行ってよい。`deliveries/`限定の制約は撤廃した。
3. **バージョン確定＝コミット＋タグ、の原則自体は維持する。** バージョンを確定する際は、コミットに続けて同名タグ（例：`ver21`）を打ち、`modular/VERSION.txt` とタグを常に一致させる（片方だけの更新は禁止）。誰が確定作業を行う場合もこの原則には従う。
4. **2台運用・複数ツール運用の同期：** 作業開始時に必ず `git pull`、作業終了時に必ず `git push` する。Claude Code・GPT Work・Claude Coworkのいずれから作業する場合も同様。複数ツールが同時並行で書き込むため、pull忘れによる分岐・上書き事故のリスクは従来より高くなっている点に注意。
5. **コミット前チェック：** コミット前に必ず `git status` を確認する。意図しない差分（他ツールの未pull分との衝突等）がないか確認してからコミットする。

## GitHub Pages 公開ルール（2026-07-21確定）

1. **公開URL：** https://gojioki.github.io/cebu-oyako-top/ 。`main` ブランチのルート `index.html` がそのまま配信される。
2. **`index.html` を手で編集しない。** `modular/scripts/build.rb` が `modular/sections/` と `modular/css/site.css` から毎回自動生成する。手編集は次回ビルドで上書きされる。
3. **CSS・画像はリポジトリ内をルート相対で参照する**（`modular/css/site.css`・`assets/...`）。画像埋め込みの統合版（約17MB）は `.gitignore` 対象のため公開には使わない。
4. **公開の更新手順：** マスター修正 → ビルド → `index.html` を含めてコミット → push。push した内容がそのまま公開に反映される。
5. **push は公開反映と同義。** 検証前・未確定の状態を push しない。バージョン確定（コミット＋タグ）と同じ慎重さで扱う。
6. **確認セット：** 公開前に `index.html` の「未解決プレースホルダー0／参照画像の実在／CSSパス正常」を確認する。
